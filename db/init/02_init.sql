\c bperf bperf

CREATE TABLE organizations (
  id UUID NOT NULL,
  PRIMARY KEY(id)
);

CREATE TABLE users (
  id UUID NOT NULL,
  PRIMARY KEY(id)
);

CREATE TABLE organizations (
  id UUID NOT NULL,
  PRIMARY KEY(id)
);
